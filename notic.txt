/*
static  将变量变为静态变量，内存存放在全局区
const   所修饰的全局变量成为全局常量，内存存放在全局区
        所修饰的局部变量成为局部常量，内存存放在局部区
""      用双引号引起来的皆为常量，内存存放在全局区
global  全局
local   局部
全局区和代码区在程序运行前已存在
代码区特点：共享、只读
栈区：程序运行后被创建    不要返回局部变量的地址，栈区开辟的数据由编译器自动释放
运用new关键字使数据存在堆区：new int（10）
delete负责释放堆区：delete p；
int& a（）的函数可以作为左值
*/